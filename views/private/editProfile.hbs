{{!-- <h1>Edit profile</h1>

<form action="/edit-profile/{{user._id}}" method="post" enctype="multipart/form-data">
  <label for="username">Username:</label>
  <input type="text" name="username" id="username" value="{{user.username}}"/>

  <label for="bio">Bio:</label>
  <textarea class='form-control' name='bio' id='bio' cols='10' rows='5'>{{user.bio}}</textarea>

  <label for="image">Select an image for your profile!</label>
  <input type="file" name="image" id="image" />

  <button type="submit">Update profile</button>

</form>

 --}}
<link rel="stylesheet" href="/stylesheets/edit-profile.css">



<nav class="navbar navbar-expand-lg" style="background-color:rgb(20, 99, 86)">
  <div class="container-fluid">
    <a
      class="navbar-brand"
      style="color:rgb(255, 241, 189); margin-right: 70vw;"
      href="/auth/signup"
    >ReMeals</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/recipes">Recipes
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="/profile">Profile</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/auth/logout">Logout</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="center">
<form action="/edit-profile/{{user._id}}" method="POST" enctype="multipart/form-data">
<div class="container">
        <div class="card">
            <a class="singup">Edit profile</a>
            <div class="inputBox1">
                <input type="text"  name="username" value="{{user.username}}">
                <span class="user">Username </span>
            </div>

            <div class="inputBox1">
                <input type="text"  name="bio" value="{{user.bio}}">
                <span class="user">Bio </span>
            </div>

            <div class="inputBox">
                <input type="file" name="image" id="image" />
                <span>Profile picture</span>
            </div>



            <button type="submit" class="enter">Set changes</button>

        </div>
    </div>
    </div>
    </form>
